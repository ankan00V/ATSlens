# Dispatch for Explorer M3-1

Investigate `frontend/src/` (specifically `Hero.tsx` and related components/APIs) and backend API endpoints (`/api/analyze`, `/api/export-pdf`).
Determine exact requirements, component state, prop interfaces, and DOM structure needed for:
1. Selectable demo resume presets ("Google Senior Frontend Engineer", "Meta Backend Engineer", "AI Research Intern").
2. Sub-score progress bars (`Experience`, `Skills`, `Education`, `Impact`) and Missing Tech Stack badges.
3. PDF Evaluation Summary download button calling `POST /api/export-pdf`.
