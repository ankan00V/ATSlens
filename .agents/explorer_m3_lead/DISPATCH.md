# Dispatch for Explorer M3 Lead

Investigate `frontend/src/` (especially `Hero.tsx` and relevant components/APIs) and backend API contracts (`/api/analyze`, `/api/export-pdf`).
Cover all three aspects of Milestone 3:
1. Component structure and state changes required in `Hero.tsx` for selectable demo resume presets ("Google Senior Frontend Engineer", "Meta Backend Engineer", "AI Research Intern"). Provide precise preset text & job descriptions.
2. Rendering sub-score progress bars (`Experience`, `Skills`, `Education`, `Impact`) and Missing Tech Stack badges based on `/api/analyze` response.
3. PDF Evaluation Summary download button triggering `POST /api/export-pdf` and handling blob download response in client JS.
4. Verify design compliance (MAANG aesthetics, dark theme without pure black #000000, Geist font, responsive layout).

Deliver a complete, step-by-step implementation strategy for the Worker in your handoff report (`/Users/ankanghosh/Desktop/projects/ATSlens/.agents/explorer_m3_lead/handoff.md`).
