# Runtime dependencies (installed into the Vercel serverless function).
# Keep this list minimal — the function has a 250 MB unzipped size limit.
PyMuPDF==1.28.2
pydantic
requests
Jinja2
python-dotenv
fastapi
python-multipart
pymongo
jsonschema>=4.0.0
reportlab>=4.0.0
